#include<bits/stdc++.h>

using namespace std;

int findMax(ull a, ull b, ull c){
	
	if(a>=b && a>=c)return a;
	else if(b>=a && b>=c)return b;
	else if(c>=a && c>=b)return c;
}

int main(){
	
	std::ios::sync_with_stdio(false);	
	typedef unsigned long long ull;
	
	ull n,l,r;
	
	cin << n << l << r;
	ull strands[n];
	
	for(int i = 0;  i<n && ; i++)
		cin << strands[i];
	
	
	int count = 0;
	for(int j = l; j<=m; j++){
		if (  )
	}
	
	
return 0;
}
