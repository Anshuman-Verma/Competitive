#include<bits/stdc++.h>

#define newl "\n"
#define MODULO 1000000007

using namespace std;

int main(){
	std::ios::sync_with_stdio(false);
	
	int n,m;
	cin >> n >> m;
	map<int, int> route;
	while(m--){
		int a,b;
		cin >> a >> b;
		route[a] = b;
	}
		
		
	
return 0;
}
